// Legacy script placeholder — when we used an iframe this file powered
// navigation. We now use standard multi-page navigation with web components
// for the shared header/footer. Keep this file minimal for future UI hooks.
function initUI() {
    // Placeholder for UI helpers if needed later
}

document.addEventListener("DOMContentLoaded", initUI)
